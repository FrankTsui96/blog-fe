server {
    listen       80;
    server_name  localhost;

    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # 核心：支持 SPA 路由，找不到文件就回退到 index.html
        try_files $uri $uri/ /index.html;
    }
}